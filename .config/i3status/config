general {
        colors = true
        interval = 5

}


order += "volume master"
order += "disk /"
order += "wireless _first_"
#order += "ipv6"
order += "ethernet _first_"
order += "battery all"
order += "load"
order += "cpu_temperature 0"
order += "tztime local"

volume master {
        format = "♪: %volume"
        format_muted = "♪: muted (%volume)"
	device = "default"
        mixer = "Master"
        mixer_idx = 0
}

wireless _first_ {
        format_up = "📶 %ip"
        format_down = "📶 offline"
}

ethernet _first_ {
        format_up = "🖧  %ip"
        format_down = "🖧  offline"
}

cpu_temperature 0 {
        format = "CPU-Temperature: %degrees °C"
	max_threshold = 80
	format_above_threshold = "WARNING: Temperature above threshold: %degrees °C"
        path = "/sys/devices/platform/coretemp.0/hwmon/hwmon2/temp1_input"
}

battery all {
        format = "%status %percentage %remaining"
	last_full_capacity = true
	status_chr = "⚡ CHR"
        status_bat = "🔋 BAT"
        status_unk = "? UNK"
        status_full = "🔋FULL"
        low_threshold = 10
}

tztime local {
        format = "%d.%m.%Y %H:%M"
}

load {
        format = "CPU-Load: %1min %"
}

disk "/" {
        format = "Free Disk Space: %avail"
}

